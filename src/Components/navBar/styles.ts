import styled from 'styled-components';

export const Container = styled.header`
    background: #fff;
    display: flex;
    width: 100%;
    height: 4rem;
    padding: 0.5rem 1.5rem;
    align-items: center;
    position: fixed;
    `

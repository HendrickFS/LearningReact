import styled from "styled-components";

export const Container = styled.div`
    display: flex;
    position: absolute;
    top: 60%;
    right: 10px;
    gap: 10px;
    img{
        width: 1.5rem;
        height: 1.5rem;
    }
    button{
        background: white;
        opacity: 0.7;
        width: 2rem;
        height: 2rem;
        border-radius: 50%;
    }
`